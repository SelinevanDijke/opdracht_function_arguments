const paintWall = function (muur, verf) {
    console.log("De " + muur + " muur is " + verf + " geverfd.");
};

const kantMuur = "rechter";
const kleurVerf = "blauw";

paintWall(kantMuur, kleurVerf);

// Wanneer er geen argument toe wordt gevoegd wordt er geen kleur getoond. 
// De volgorde maakt uit wanneer de functie wordt opgeroepen.
//  Wanneer je de argumenten switcht komen ze andersom te staan


